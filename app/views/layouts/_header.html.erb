<nav class="navbar navbar-madiba navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">MADIBA</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <div class="col-sm-6 col-md-6">

        <%= form_tag(books_path, :method => "get", id: "search-form", class: "navbar-form", role: "search") do %>
            <div class="input-group">
              <%= text_field_tag :search, params[:search], placeholder: "Buscar...", class: "form-control", name: "search" %>
              <div class="input-group-btn">
                <%= button_tag(type: "submit", class: "btn btn-default") do %>
                  <i class="fa fa-search"></i>
                <% end %>
              </div>
            </div>
        <% end %>
      </div>
      <ul class="navbar-form navbar-right">
        <form>
          <% if user_signed_in? %>
              <%= link_to "Edit profile", edit_user_registration_path, class: 'btn' %> |
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'btn'  %>
          <% else %>
              <%= link_to "Sign up", new_user_registration_path, class: 'btn'  %> |
              <%= link_to "Login", new_user_session_path, class: 'btn'  %>
          <% end %>
        </form>
      </ul>
    </div><!--/.navbar-collapse -->
  </div>
</nav>

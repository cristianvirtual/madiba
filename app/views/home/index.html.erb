<div class="col-lg-3">
  <!-- bloque de usuario -->
  <% if user_signed_in? %>
  <%= render 'user_card' %>
  <% end %>
  <!-- bloque reqyest -->
  <%= render 'book_request' %>
</div>
<div class="col-lg-9">
  <!-- Mis libros alojados contents  -->
  <%= render 'book_hosting' %>
  <!-- Libros pasados  -->
  <%= render 'book_hosted' %>
  <!-- Libros donados -->
  <%= render 'book_donated' %>

  <!-- Libro detalle y timeline -->
  <%= render 'book_detailed' %>
</div>
